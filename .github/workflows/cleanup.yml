name: Cleanup old artifacts

on:
  workflow_run:
    workflows: ["Build with NodeJS and Webpack"]
    types:
      - completed

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: c-hive/gha-remove-artifacts@v1
        with:
          age: '10 second'  # deletes everything except the most recent
