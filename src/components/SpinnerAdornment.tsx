import { CircularProgress } from "@mui/material";
import React from "react";

const SpinnerAdornment = () => (
  <CircularProgress size={15} color="inherit" sx={{ ml: 2 }} />
);

export default SpinnerAdornment;
